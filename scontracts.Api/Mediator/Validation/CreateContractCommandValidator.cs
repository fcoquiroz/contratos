using FluentValidation;
using scontracts.Api.Mediator.Commands;
using System.Collections.Generic;


namespace mer.monitor.organization.Validation
{
    /// <summary>
    /// CreateContractCommandValidator
    /// </summary>
    public class CreateContractCommandValidator 
    {
       /* public CreateContractCommandValidator()
        {

            //RuleFor(x => x.Name)
            //     .NotEmpty().WithMessage("El nombre del usuario es requerido.")
            //     .Length(1, 40).WithMessage("El nombre del usuario debe contener hasta 40 caracteres.");


          

        }
        */
        //private bool BeValidationListUserTypes(List<UserType> UserTypes)
        //{
        //    int countUserTypes = UserTypes.Count;
        //    if (countUserTypes == 1)
        //    {
        //        countUserTypes = UserTypes[0].UserTypeId;
        //    }

        //    if (countUserTypes > 0) { return true; } else { return false; }
        //}

    }
}